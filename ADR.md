# 開発日誌

## 使用言語
* **決定** : Java
* **理由** :
    * 将来的にWebアプリケーション化することを考えており、Webアプリとしての使用率が高かったため
    * 採用率が高く、実務を見据えたスキル習得が見込めるため

## フレームワーク
### 2025-08-12
* **決定** : JavaFX
* **理由** : 
    * Java標準ライブラリの一部として学習コストが低く、手軽にGUIアプリを作成できるため
    * ローカル環境だけで完結して動作させることができるため

### 2026-02-05
* **決定** : Spring Bootへの移行
* **理由** :
    * JavaFXでは配布・更新のハードルが高く使用率が伸びないと考えたため
    * タイピングアプリの性質上、メインターゲットがパソコン初心者になるため環境構築が不必要なWebアプリにするため

## テスト
### 2026-02-10
* **決定** : JUnit5による単体テストの導入
* **理由** : リファクタリング時のデグレを防止し、機能追加を安全に行うため
* **手法** : パラメタライズドテスト、境界値テスト、Mockitoによるモック化

## データモデル
* **決定** : `record`型を採用
* **対象** : `WordResult`, `TestResult`などのデータ保持クラス
* **理由** :
    * クラスをまたいで共有したい関連データを一つのオブジェクトとしてまとめるため
    * データが作成後に意図せず書き換えられることを防ぎ、バグの混入リスクを減少させるため
    * 通常のクラスで必要になるSetter/Getterクラスなどの定型コードを自動的に生成し、可読性を高めるため

## アルゴリズム選定
**対象** `MistakeAnalyzer`
### 誤答判定
* **決定** : ダメラウ・レーヴェンシュタイン距離
* **理由** : 
    * 検討した方法はレーヴェンシュタイン距離とダメラウ・レーヴェンシュタイン距離、機械学習による判定の3つである。本アプリは、自分のタイプミスの傾向を確認することでより効率的な能力向上を目的としているため、ユーザーになぜそのミス判定になったかを示す必要がある。そのため判定根拠が不透明かつ学習データに依存して挙動が変わる機械学習を除外した。また、レーヴェンシュタイン距離では交換ミスの判定をできないためレーヴェンシュタイン距離を除外した。
    * 単語の文字数を$N$, $M$としたとき計算量は$O(N \times M)$であり、入力文字数が少ないことからリアルタイム処理でもパフォーマンスに問題がないため

### ミス判定の順序
* **決定** : 置換 > 交換 > 削除 > 挿入の順で優先する
* **理由** : 自分のタイプミスを分析すると、この順番でミスが多かったため

## データ構造選定
### 分析結果の保持
* **決定** : `LinkedHashMap`を採用
* **対象** : `TypingAnalyzer`
* **理由** : 
    * 元の単語とミスの種類のリストを結び付けて保存しておきたいため
    * ユーザーが入力した時系列順のまま保存しておきたいため

### 練習問題の保存方法選定
* **決定** : `Set`を採用
* **対象** : `PracticeService`
* **理由** :
    * 重複を許さないという仕様をデータ構造で担保しているため
    * `List`で管理して`contains`で確認するより、`Set`の方が計算量が$O(1)$と高速であるため

## Stream APIの採用
* **対象** : `WeaknessAnalyzer`, `PracticeService`
* **理由** : 将来的にデータが膨大になった際、並列化することで実行時間を減少できるため

## 日時の判定方法
* **決定** : `LocalDateTime`を採用
* **対象** : `ResultFilter`
* **理由** : 
    * 不変性を持つため
    * 日付の加算・比較が容易で、ロジックミスを防ぐため

## `MistakeDetail`の簡略化
### 2026-02-27
* **決定** : `MistakeDetail`から`position`フィールドの削除
* **対象** : `MistakeDetail`
* **理由** : 現状使用しておらず、活用方法が見い出せないため

## データベース選定
### 2026-03-12
* **決定** : H2 Databaseを採用
* **比較対象** : MySQL, PostgreSQL, H2
* **理由** : 
    * 本アプリは個人学習が目的であるため、サーバ再起動後にデータが消えても問題ないため
    * H2は依存関係に追加するのみで動作し、MySQLなどと比較して環境構築が不要のため

## API設計
### 2026-03-12
* **決定** : REST APIを採用
* **比較対象** : Thymeleaf, REST API
* **理由** : 
    * フロントエンドとバックエンドを疎結合にすることで、それぞれを独立して開発及びリファクタリングできるため
    * 正確なWPMを測定するために、クライアント側で経過時間を計測しそのデータのみをサーバに送るステートレスな形が最適であるため

## Swagger UIの導入
### 2036-03-12
* **決定** : Swagger UIを導入
* **理由** : 
    * REST APIの動作確認のために、毎回curlコマンドを使う手間を省略するため
    * APIの設計変更が即座に反映されるため、仕様書の変更が不必要になるため

## `LinkedHashMap<String, WordResult>`の永続化方法
### 2026-03-12
* **決定** : JSON文字列(`@Lob`+`ObjectMapper`)
* **対象** : `TestResultEntity`
* **理由** : 
    * `record`の不変性
    * `@ElementCollection`を使用した場合、ネスト構造によるテーブルの増加が見込まれるため
    * 既存クラスへの変更不要

### 2026-03-21
* **決定** : `resultsJson`を`WordResultEntity`+`MistakeDetailEntity`に正規化する
* **対象** : `TestResultEntity`
* **理由** :
    * SQLで直接検索できるようにするため
    * 特定のミスタイプだけ表示など、フィルターを導入する際に効率的にするため

## 現在時刻の型
### 2026-03-24
* **決定** : `startedAt`をlong型で運用する
* **対象** : `TestStartResponse`
* **理由** : 
    * longであればJavaScript側で`Date.now() - starteAt`で直接`usedTimeMillis`を計算できるため
    * LocalDateTimeはJacksonがナノ秒精度でシリアライズするためJavaScriptの`Date.now()`と誤差が生じる可能性があるため
* **注意点** : サーバーとクライアントは同一環境であることを想定している

## `usedTimeMillis`測定方法
### 2026-03-21
* **決定** : クライアント側で計算し、`AnalyzeRequest`で送信する(`TestSession`不使用)
* **理由** : RESTはステートレスのためリクエスト間で状態を保持できないため

## フロントエンドの開発方針
### 2026-03-24
* **決定** : HTML, CSS, JavaScriptの実装はClaude Codeに一任する
* **理由** : 
    * フロントエンドのUI実装に時間を割くより、バックエンドの品質を高めることが自身のバックエンドエンジニア志望と合致するため
    * フロントエンドは最低限の品質で良く、それを自力で1から実装する必然性はないため

## 練習結果の取り扱い
### 2026-03-28
* **決定** : 練習結果は分析のみ行い、データベースには保存しない
* **対象** : `PracticeController`
* **理由** : 
    * 練習問題は弱点に特化した単語で構成されるため、実力より正答率が低くなる傾向があり、弱点分析の精度がゆがむため
    * 出題条件の異なるデータが混ざることでWPMや正答率の変化を追跡する際、比較する意義が薄れるため
